export enum ButtonSizeEnum {
    MD = "md",
    SM = "sm",
}

export enum ButtonVariantEnum {
    DANGER = "danger",
    GHOST = "ghost",
    GHOST_DANGER = "ghost-danger",
    LINK = "link",
    MUTED = "muted",
    PRIMARY = "primary",
}
